# VividHome (3d-house)

Run the app from **`frontend/`** (Next.js). Meshy HTTP helpers live in **`backend/`**.

```bash
cd frontend && npm install && npm run dev
```

Set `frontend/.env.local`: `MESHY_API_KEY=...`
